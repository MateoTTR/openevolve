# Session 001 — 2026-03-28

**Sprint**: 001 — Quick Wins + CI Foundation
**Duration**: ~2h
**Status**: Sprint 001 complete

---

## Accomplished

### Planning & Design
- Analyzed reasoning model detection architecture in `openai.py`
- Identified that prefix-based detection is unreliable for non-OpenAI models (Gemini thinking is optional, naming unpredictable)
- Designed 3-state `is_reasoning_model` config flag solution
- Confirmed Claude models should NOT be added to reasoning prefix list (different API params)
- Established upstream PR workflow: branch from `main`, merge into `develop` (not cherry-pick)
- Set up memory system for future sessions

### Implementation
- **PR #3** — CI workflow (`.github/workflows/fork-ci.yml`)
  - ubuntu-latest runner, Python 3.10
  - Unit tests, black, isort, mypy (lint steps as `continue-on-error` due to upstream formatting)
  - `timeout-minutes: 15`

- **PR #4** — Reasoning model config
  - Extracted `OPENAI_REASONING_MODEL_PREFIXES` to module-level constant
  - Created `is_reasoning_model(model_name, config_flag)` function
  - Added `is_reasoning_model: Optional[bool] = None` to `LLMModelConfig`
  - Cleaned up redundant prefixes, replaced `gpt-oss-120b`/`gpt-oss-20b` with `gpt-oss-` prefix
  - Rewrote stale `test_openai_model_detection.py` (was testing non-existent `api_base` logic)
  - 12 new tests, 384 total passing

### Code Reviews
- PR #3: 3 critical findings (black/isort/mypy fail on upstream code) + 1 suggestion (timeout)
- PR #4: 2 warnings (redundant prefixes, stale test) + 2 suggestions (gpt-oss coverage, shared_config comment)
- All findings addressed and verified

### Infrastructure
- Enabled GitHub Issues on fork repo
- Created issues #1 (CI) and #2 (reasoning model config)
- Set up `settings.local.json` with permissions from Revolver template
- Both PRs merged into `develop`

---

## Decisions Made

1. **No Gemini/Claude prefixes in hardcoded list** — use explicit `is_reasoning_model` config flag instead
2. **Lint steps as `continue-on-error`** — cannot format 41 upstream files without breaking cherry-pick compatibility
3. **`ubuntu-latest` only** — no self-hosted runner for CI
4. **Branch from `main` for upstream PRs** — never cherry-pick from `develop`

---

## Open Items

- [ ] Tag `v0.2.27-rev.1` on `develop`
- [ ] Submit `upstream/reasoning-model-config` as PR to upstream repo
- [ ] Investigate MCP GitHub setup for inline code reviews (currently using `gh` CLI)
- [ ] Commit session docs to `develop`

---

## Files Changed

| File | Change |
|---|---|
| `.github/workflows/fork-ci.yml` | New — CI workflow |
| `openevolve/llm/openai.py` | Extract prefix constant + `is_reasoning_model()` function |
| `openevolve/config.py` | Add `is_reasoning_model` field + `shared_config` comments |
| `tests/test_openai_model_detection.py` | Rewritten to use real function |
| `tests/test_reasoning_model_detection.py` | New — 12 tests for 3-state logic |
